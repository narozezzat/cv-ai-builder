/**
 * Public surface of the dashboard feature — the signed-in shell and the panels the
 * dashboard page composes. Same contract as the other barrels: routes reach this
 * file and nothing deeper.
 */

export { AppHeader } from "./components/app-header";
export { AppNav } from "./components/app-nav";
export { SettingsNav } from "./components/settings-nav";
export { StatCards } from "./components/stat-cards";
